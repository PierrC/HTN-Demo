using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Alcove : MonoBehaviour {

    // This script doesn't nothing
    // Is just used to make sure gameobject 
    // is alcove
    private void Update() {

       // Debug.Log("Alcove transform.childCount " + gameObject.transform.childCount);
    }
}
